---
run_id: gateway_embedder

nodes:
  - number: 0
    profile: configs/gateway_with_embedding.yaml
    env:
      GATEWAY_BATCH_SIZE: 8
      ENABLE_ADAPTIVE_BATCHING: false
  - number: 1
    profile: configs/retrieval_only.yaml
  - number: 2
    profile: configs/generation.yaml

workload:
  requests: 60
  concurrency: 1

notes: Gateway hosts embedder; retrieval handles FAISS only.
