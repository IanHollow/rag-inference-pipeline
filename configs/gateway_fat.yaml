---
name: gateway_fat
description: Gateway Node with Embedding and Reranker

components:
  - name: orchestrator
    type: gateway
    # URLs are derived from NODE_1_IP and NODE_2_IP environment variables
  - name: embedding_generator
    type: embedding
  - name: reranker
    type: reranker

routes:
  - prefix: /
    target: gateway
