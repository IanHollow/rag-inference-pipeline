---
name: generation_llm_only
description: Generation node with only LLM (no Rerank, Sentiment, Toxicity)

components:
  - name: llm_generator
    type: llm

routes:
  - prefix: /generate
    target: generation
