# Shared environment variables for all nodes
# Edit these values to configure the pipeline

# Node Configuration
export TOTAL_NODES=3
export NODE_0_IP=localhost:8000
export NODE_1_IP=localhost:8001
export NODE_2_IP=localhost:8002

# Data Configuration
export FAISS_INDEX_PATH=faiss_index.bin
export DOCUMENTS_DIR=documents/
export ONLY_CPU=true
