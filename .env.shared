# Shared environment variables for all nodes
# Edit these values to configure the pipeline

# Node Configuration
TOTAL_NODES=3
NODE_0_IP=localhost:8000
NODE_1_IP=localhost:8001
NODE_2_IP=localhost:8002

# Data Configuration
FAISS_INDEX_PATH=faiss_index.bin
DOCUMENTS_DIR=documents/
ONLY_CPU=true
